name: Nightly-Tests

on:
  schedule:
    - cron: '0 5 * * *'  # Runs every day at midnight EST (UTC-5)
  workflow_dispatch:  # Allows manual trigger as well

jobs:
  Longer-Functional-Test:
    runs-on: [self-hosted, Linux, X64]
    name: Longer Functional
    steps:
      - uses: actions/setup-go@v3
        with:
          go-version: 1.19
      - uses: actions/checkout@v3
      - name: Build Siglens
        run: make build
      - name: Run Siglens
        run: |
            ./siglens --config server.yaml &
      - name: Run Longer Functional Test
        run: |
            sleep 5  # Wait for Siglens to start
            cd tools/sigclient
            go run main.go functional -d http://localhost:8081/elastic -f longerFunctionalQueries/longerFunctionalQueries.yml -q localhost:5122 -l true
  
  Clickbench-10M-Test:
    runs-on: [self-hosted, Linux, X64]
    name: Clickbench 10M
    steps:
      - uses: actions/setup-go@v3
        with:
          go-version: 1.19
      - uses: actions/checkout@v3
      - name: Build Siglens
        run: make build
      - name: Run Siglens
        run: |
            ./siglens --config server.yaml &
      - name: Ingest Data
        run: |
            cd tools/sigclient/clickbench
            wget https://github.com/siglens/pub-datasets/releases/download/v1.0.1/splithits_aa.tar
            tar -xvf splithits_aa.tar
            rm splithits_aa.tar
            python3 send_datamulti.py
      - name: Restart Siglens
        run: |
            sleep 10
            pkill siglens
            cd ../../..
            ./siglens --config server.yaml &
      - name: Run Longer Functional Test
        run: |
            sleep 5  # Wait for Siglens to start
            cd tools/sigclient
            go run main.go clickBench -d localhost:5122



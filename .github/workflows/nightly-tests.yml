name: Nightly-Tests

on:
  schedule:
    - cron: '0 5 * * *'  # Runs every day at midnight EST (UTC-5)
  workflow_dispatch:  # Allows manual trigger as well

jobs:
  Clickbench-10M-Test:
    runs-on: [self-hosted, Linux, X64]
    name: Clickbench 10M
    steps:
      - uses: actions/setup-go@v3
        with:
          go-version: 1.19
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12.7'  # or whatever version you're using
      - uses: actions/checkout@v3
      - name: Build Siglens
        run: make build
      - name: Run Siglens
        run: |
            ./siglens --config server.yaml &
      - name: Ingest Data
        run: |
            python -m pip install --upgrade pip  # Ensure pip is upgraded
            pip install requests  # Install the `requests` library
            cd tools/clickbench
            python3 send_datamulti.py
      - name: Restart Siglens
        run: |
            sleep 10
            pkill siglens
            ./siglens --config server.yaml &
      - name: Run Longer Functional Test
        run: |
            sleep 5  # Wait for Siglens to start
            cd tools/sigclient
            go run main.go clickBench -d localhost:5122



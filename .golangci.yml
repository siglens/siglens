issues:
  exclude-rules:
    - linters:
        - staticcheck
      text: "SA6002:"

linters:
  enable:
    - exhaustive
    - importas

linters-settings:
  exhaustive:
    default-signifies-exhaustive: true
  importas:
    alias:
      - pkg: github.com/siglens/siglens/pkg/segment/utils
        alias: segutils

run:
  build-tags:
    - e2e_all
